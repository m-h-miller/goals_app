<h1>Goals</h1>

<ul>
<% @goals.each do |goal| %>

  <% if goal.private = false || current_user.id == goal.user_id %>
    <li>
      <a href="<%= goal_url(goal) %>"  >
        <%= h(goal.title) %>
      </a>
      <br>
      status: <%= goal.status %>  
    </li>
  <% end %>

<% end %>
</ul>
